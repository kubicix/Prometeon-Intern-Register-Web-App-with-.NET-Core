@model IEnumerable<StajBasvuru2.Models.BasvuruKisisel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adsoyad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adsoyad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Tcno">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Tcno">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Tcno">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
